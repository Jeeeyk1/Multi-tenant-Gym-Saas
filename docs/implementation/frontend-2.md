Create a full-screen animated welcome/splash screen component with the following:

DESIGN:
- Background: #0F0F0F
- Centered layout, vertically and horizontally

LOGO ANIMATION SEQUENCE:
1. Start with a cartoon chicken mascot icon (emoji or SVG placeholder 🍗) 
   fading in from scale 0.5 to 1.0 with a slight bounce — duration 600ms
2. App name "GainzOS" fades in below it with a slide-up effect — delay 400ms
3. Tagline "Train. Connect. Grow." appears letter by letter — delay 800ms
4. Subtle mint green (#6EE7B7) glow pulse around the logo — loops once

PROGRESS:
- Thin loading bar at the bottom, mint green (#6EE7B7), animates from 0% to 100% over 2 seconds
- After complete, entire screen fades out and routes to the main dashboard

TECH:
- Use Framer Motion for animations if React
- Pure CSS keyframes if plain HTML/CSS
- No external animation libraries unless Framer Motion is already installed

Make it feel premium and smooth, not flashy or overdone.